services:
 fronted:
  container_name: frontend1234@
  image: web-ui-image:latest
  ports: 
   - "5174:5174"
  networks:
    - mynetwork
  deploy:
   replicas: 2
networks:
  mynetwork:
    driver: overlay
